/**
 * @human-machine-judgment/storage-sqlite
 *
 * SQLite storage adapter for durable local persistence of Judgment Points.
 * Provides a persistent, file-based storage implementation backed by SQLite
 * for reliable single-node deployments and local development.
 */

export const VERSION = '0.1.0';
